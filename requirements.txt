python-telegram-bot==21.7
Flask==3.0.3
requests==2.32.3
beautifulsoup4==4.12.3
APScheduler==3.10.4
python-dotenv==1.0.1
discord.py==2.4.0

# Optional - only needed if you use the /ask command or the scraper's AI
# fallback. Safe to leave installed even if you never use either; costs
# nothing unless called.
google-genai>=1.0.0
